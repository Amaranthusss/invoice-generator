import Grid from '../../common/grid/Grid'

import { gridOptions } from './ServicesList.options'

const Services = (): JSX.Element => {
  return <Grid options={gridOptions} />
}

export default Services

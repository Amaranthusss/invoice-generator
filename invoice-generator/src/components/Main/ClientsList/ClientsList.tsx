import Grid from '../../common/grid/Grid'

import { gridOptions } from './ClientsList.options'

const ClientsList = (): JSX.Element => {
  return <Grid options={gridOptions} />
}

export default ClientsList
